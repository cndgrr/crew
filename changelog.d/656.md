### Added

- The drill's `fleet` leg proves `crew restart --all`, `crew down` and `crew reset --cut --all` across the round's own roster, naming each box's outcome and reading the verbs' output rather than their exit code (#656).
