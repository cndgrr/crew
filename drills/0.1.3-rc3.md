# crew 0.1.3-rc3 drill record — cut 2026-09-13

This is the record required before the candidate cut, not a claim that the
real-host round has already run. The round runs after merge against the
published `0.1.3-rc3` tag, and its readings are appended here before
promotion. This is not a waiver: crew#731 Part 3 names the round and Part 4
names the promotion gate it feeds.

## The candidate

Candidate engine anchor: `build/731-release-0-1-3-rc3` at
`289bd36bb3be5e447ecf0af6773bbbb2fea48888`, cut from `main` at
`6f0b76ae13da190363cd97047cad00abc502e3cb` (2026-09-13). Every commit after
that anchor changes only this record, so `git diff 289bd36 HEAD --
':!drills/0.1.3-rc3.md'` is empty and the engine tree named here cannot drift
as its evidence is completed. The release doors publish the final merge tree
as the prerelease tag `0.1.3-rc3` and re-arm `main` as `0.1.3-rc4-dev`.

The tree drilled for the preceding candidate is the published
`0.1.3-rc2` tag at `2a8f40e24ac10e7ff7641ac31fd36bfe35bcec1e`.
The engine-tree diff from that immutable anchor is the following 24 paths.
The issue citations identify the prerequisite whose merged tree supplies
each change, and each was read from that path's merge commit on `main`
rather than from any prose.

| path | change since `0.1.3-rc2` |
| --- | --- |
| `drill/rehearsal-attention-audit.sh` | mint the audit's board vocabulary from the box's own effective `LABEL_*` (#735) |
| `drill/rehearsal-attention.sh` | mint and match the dispatch vocabulary from the box, and load the queue from `fleet.conf` (#735) |
| `drill/rehearsal-breaker.sh` | grade the fixture state and the tick execution rather than the request's exit status (#724); read the refused tick's holder diagnostic (#726) |
| `drill/rehearsal-fixtures.sh` | mint fixtures under the box's effective labels (#735); provision the builder's sandbox fork (#727) |
| `drill/rehearsal-resume.sh` | ask the engine's per-tick roll-call whether this PR was dispatched for, and cut orphaned claims from that scan (#725) |
| `drill/rehearsal-safety.sh` | refuse a phase-2 box identity equal to the host identity, naming both (#729) |
| `drill/rehearsal.sh` | mint the board vocabulary from the box (#735); refuse the identity collision at the top of phase 2 (#729); create the builder's fork before its leg (#727) |
| `drill/teardown.sh` | track and tear down the builder box's sandbox fork, preserving a fork that cannot be inspected (#727) |
| `shared/bin/tick.sh` | a refused tick asks the kernel who holds the lock and distinguishes a live holder from an inherited descriptor (#726) |
| `shared/conf/agents/claude.conf` | `bot_session_productive` reads the killed Claude session's own transcript (#723) |
| `shared/conf/agents/kimi.conf` | `bot_session_productive` reads the killed Kimi session's own `wire.jsonl` artifact (#746) |
| `shared/lib/common/identity.sh` | the auth-failure reason records the matched line alone, not the line and the exit-code fallback (#736) |
| `shared/lib/common/session.sh` | the profile hook's productivity answer outranks the stdout classifier; absent or `cannot tell` preserves the shipped fallback (#723) |
| `shared/lib/common/tick-health.sh` | carry the holder diagnostic's wording for the refused tick (#726) |
| `shared/lib/duty-builder.sh` | the authored-PR awareness pass is bounded by the registry; the unscoped search is removed rather than narrowed (#728) |
| `shared/docs/rehearsal.md` | document the phase-2 identity refusal (#729) and the breaker leg's evidence grading (#724) |
| `shared/test/builder.sh` | cover the registry-bounded authored pass (#728) and replay the resume leg's six ticks through the real lane composition (#725) |
| `shared/test/common.sh` | mutation guards for the board vocabulary (#735), the registry bound (#728), the holder diagnostic (#726), the resume roll-call split (#725), the builder fork (#727) and the breaker's evidence grading (#724) |
| `shared/test/common/identity.sh` | pin the matched-line auth-failure reason (#736) |
| `shared/test/common/session.sh` | pin the profile hook's precedence over the stdout classifier (#723) |
| `shared/test/common/tick-health.sh` | pin the refused tick's holder wording (#726) |
| `shared/test/conf.sh` | drive the Kimi wire artifact's productivity classes (#746) |
| `shared/test/drill-teardown.sh` | exercise builder-fork discovery and teardown against a stub box (#727) |
| `shared/test/drill.sh` | exercise the effective label vocabulary and queue loader (#735), the phase-2 identity refusal (#729), the resume roll-call (#725) and the breaker's evidence grading (#724) |

Seven of those paths are engine — under `shared/lib/`, `shared/bin/` or
`shared/conf/` — so this candidate moves the installed payload and is a real
candidate rather than a harness-only re-cut.

The non-engine delta from the preceding tag is deliberately separate:

- `VERSION` moves from the re-armed `0.1.3-rc3-dev` value on `main` to the
  bare `0.1.3-rc3` candidate stamp in this cut.
- `drills/0.1.3-rc2.md` contains the issue-driven append of the rc2 round's
  readings (#730).
- This file records the rc3 candidate and the round it is owed.

`CHANGELOG.md` and every file under `changelog.d/` are byte-identical to the
merge base, as the tag-only candidate ladder requires. The first `## ` section
of `CHANGELOG.md` is `## 0.1.2 — 2026-08-10`, not a stamped rc section.

## The `0.1.3-rc2` findings — class as each landed PR states it

crew#731 Part 2 requires this table, and requires it to name every row that
differs from the findings table in `drills/0.1.3-rc2.md`. Class is measured
the way #731's own #729 row measures it: a finding is **engine** when its
merge commit moves a path under `shared/lib/`, `shared/bin/` or
`shared/conf/`, and **harness** when it does not.

| issue | class in `drills/0.1.3-rc2.md` | class as the landed PR states it | merge |
| --- | --- | --- | --- |
| #724 | harness | **harness** — PR #734: *"Two harness defects, one shape"* | `d8962f2` |
| #725 | **engine** | **harness** — PR #738: *"Harness, and one defect rather than two: both failing rows are the same over-broad predicate"* | `ae6a8b5` |
| #726 | **engine**, low severity | **engine** — PR #740: *"The engine's diff is now `tick.sh` alone"* | `b2e36e3` |
| #727 | harness | **harness** — PR #737 states no class in those words; measured harness at its merge | `b59209a` |
| #728 | **engine** | **engine** — PR #742 bounds the pass in `shared/lib/duty-builder.sh` | `52773a5` |
| #729 | harness | **harness** — PR #743 states no class in those words; measured harness at its merge | `0480eba` |

**One row differs from `drills/0.1.3-rc2.md`: #725.** That record classed it
**engine**; its landed PR #738 concludes **harness**, and one defect rather
than two. Triage
[placed the correction here](https://github.com/heavy-duty/crew/issues/725#issuecomment-5648638035)
rather than in the rc2 record, because `drills/0.1.3-rc2.md` is that round's
dated first reading and no path of PR #738 touches it. The rc2 record is
therefore left byte-identical by this cut, and this row is the correction.

The reclassification does not change what this candidate is. #726 and #728
still move installed modules, and three further members admitted to this
window — #723, #736 and #746 — move them as well, so the payload moves on
five members rather than on three.

## The admitted members — class at their merge commits

These four are not `0.1.3-rc2` findings and so are outside the table above,
which is the one crew#731 Part 2 asks for. They are recorded because they are
in this candidate's tree and three of them move the payload.

| issue | class | what moves | merge |
| --- | --- | --- | --- |
| #723 | **engine** | `shared/lib/common/session.sh`, `shared/conf/agents/claude.conf` | `9d85b46` |
| #736 | **engine** | `shared/lib/common/identity.sh` | `19e0348` |
| #746 | **engine** | `shared/conf/agents/kimi.conf` | `1ef2e13` |
| #735 | harness | nothing under `shared/lib/`, `shared/bin/` or `shared/conf/` | `6f0b76a` |

**Six members deliver files under `drill/`** — #724, #725, #726, #727, #729
and #735 — each measured on its merge commit. That is the whole count for
this cut. `drill/` is not a stamp path, so the final cannot ride
`0.1.3-rc2`; `.github/stamps-only.py` admits only `VERSION`, `CHANGELOG.md`,
`changelog.d/**` and `drills/**` between the last candidate and the final.

**#656 is not in this candidate.** @danmt removed it from this window on
[2026-09-13](https://github.com/heavy-duty/crew/issues/731#issuecomment-5653070217):
*"We cannot run #656 before -rc3 because we'd be testing something different
than what we're shipping."* The absence is visible in the tree rather than
only in the prose — `DECLARED_LEGS` in `drill/rehearsal-all.sh` carries no
`fleet` leg and there is no `drill/rehearsal-fleet.sh`, so the leg inventory
below is the same twelve legs rc2 declared. Its checks are taken by hand on
this tag, per crew#731 Part 3, and #656's own code lands after the bare
`0.1.3`.

## Offline verification at the candidate anchor

The offline suites exercise the candidate tree without a box host or live
fleet. They do not substitute for the real-host round below.

| suite | result |
| --- | --- |
| `shared/test/run.sh` | **passed 5304, failed 0** |
| `fleet-floor/test/run.sh --no-browser` | **1049 ok, 1 skipped, 0 failed** |
| `PW_CHROME=/usr/bin/chromium FLOOR_TEST_REQUIRE_BROWSER=1 fleet-floor/test/run.sh` | **1059 ok, 1 skipped, 0 failed** |

The browser-required environment makes a missing driver or browser a failure,
not a skip, so the third row is evidence the walk ran rather than evidence it
was tolerated in absence. It executed: the browser pass carries exactly ten
rows the no-browser pass does not, all of them the walk's, and two state their
own volume — `browser walk: asserted 153 checks (>= 96)` and
`browser walk (DEMO mode): asserted 21 checks (>= 14)`.

The one skip in both floor passes is
`drill: --drill-roles parses and reaches the host check`, and this record
states its reason from the harness rather than carrying
`drills/0.1.3-rc2.md`'s wording forward. That record called it *"the expected
real-host precondition: `--drill-roles` needs a real box CLI on `PATH`"*,
which is backwards in both halves. The assertion needs a box CLI to be
**absent** — it proves the flag parsed by observing the `no 'box' on PATH`
message (`fleet-floor/test/cli.sh:2350-2356`) — and what is present is not a
real box CLI but the suite's own stub: `fleet-floor/test/run.sh:79-81` exports
`PATH="$TMP/bin:$PATH"` and links `test/stub-box` to `$TMP/bin/box`, while
`cli.sh` strips only its own `$CL_TMP/bin` before the probe, so `run.sh`'s
stub survives the strip and the row skips. The skip is therefore a property
of the aggregate runner, not of this host and not of this candidate's tree,
and the count is unchanged from rc2 either way.

The release and scope gates were run locally against this tree at ceremony
pin `0.7.7` (`fc6325e762cf91d9a5723a82803d2f03059007a1`):

| guard | result |
| --- | --- |
| `changelog-armed` | version `0.1.3-rc3` agrees with the rc fragment-mode state; fragments survive |
| `changelog-monotonic` | all 3 merge-base release headings remain |
| `changelog-assembled` | no fragments consumed; `CHANGELOG.md` is byte-identical to base `6f0b76a` |
| `drill-recorded` | this non-empty rc3 record exists |
| `runner-isolated` | 8 workflows scanned; no unvouched self-hosted PR-code runner |
| `docs-sync` | 6 vendored files exactly mirror ceremony `0.7.7` |
| `stamps-only` | 76 ok, 0 failed; a candidate carries only its own record |
| `scope-coverage` | 36 ok, 0 failed; 609 tracked paths covered by 13 rows and 1 declared exception |

Every row above was re-measured against the final tree, this record included.
That matters for one of them: `scope-coverage` counts tracked paths, and this
file is one, so the figure taken before the record existed was falsified by
writing it. The 609 above is the count with this record in the tree.

`changelog-assembled` is the mechanical reading of this cut's third Part 2
clause, and `stamps-only` of the ladder rule the `drill/` count above turns
on. The fourth clause is read directly: the first `## ` section of
`CHANGELOG.md` is `## 0.1.2 — 2026-08-10`, not a stamped `0.1.3-rcN` section.

## Declared leg inventory — every leg not yet executed

Every independently runnable leg declared by `DECLARED_LEGS` in
`drill/rehearsal-all.sh` is present. None has run against the rc3 tag because
the tag does not exist until this cut merges; that named ordering is the
reason for every not-executed state, not an omission and not a waiver.

| leg | state | reason |
| --- | --- | --- |
| `hygiene` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `breaker` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `resume` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `attention` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `attention-audit` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `notify` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `installer` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `config` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `app` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `browser` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `app-armed` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |
| `teardown` | not executed | the post-publication rc3 round is owed (crew#731 Part 3) |

This inventory is transcribed from the declaration at cut time. The round's
generated `## declared leg states:` block is appended with its readings; no
generated block is invented before the instrument runs.

## The round this file is waiting for

On the drill host, with a roster file carrying at least one live armed member:

    drill/rehearsal-all.sh --ref 0.1.3-rc3 --app-roster <path>

`--ref` is not optional and its default is not this candidate:
`drill/rehearsal-all.sh` reads `INSTALL_REF="${CREW_DRILL_REF:-main}"`, and
the merge door re-arms `main` as `0.1.3-rc4-dev` the instant this cut lands.
A bare invocation would drill a tree nobody declared. `--app-roster` is not
optional either: the armed pass runs inside
`[ "$APP" -eq 1 ] && [ -n "$APP_ROSTER" ]`, and a round without it cannot
satisfy crew#731 Gate A's A1.

crew#731 Part 3 names six real-host rows, one per `0.1.3-rc2` finding, and
they are read here rather than on the defect issues:

| row | finding |
| --- | --- |
| the `breaker` leg passes on all three roles, or reports `INCOMPLETE` with a named reason, and never `FAIL` against a lane it did not reach | #724 |
| `resume: pending head remains unresumed for its tick` and `resume: unchanged head stops after the installed zero-action threshold` both pass | #725 |
| the builder leg reaches a session without a hand-made fork, and teardown leaves none behind | #727 |
| a round whose box identity equals the host identity is refused at the top of phase 2, naming both | #729 |
| no builder tick names a repository outside the registry | #728 |
| `lock contention -> 199 + message` passes; a refused tick in `duty.log` reading `lock held with no live holder` has named an exited owner and belongs here as a reading, not a failure | #726 |

Two of those rows cannot be credited from an absence. #729's row is produced
**only** by a pass that puts the host's own identity on a `builder` or
`reviewer` box; a round run entirely on separated identities cannot read it.
#728's row confirms the installed payload rather than a runtime decision,
because the engine no longer carries a line that could name an outside
repository.

The `notify` leg must **execute** rather than skip: the host needs an
operator channel credential before the round, which rc2's round did not have
(`operator channel unreachable: no-credentials`). If it is still absent, the
leg is recorded here as an operator-requested exclusion with its reason, not
as a pass.

**#656's checks are taken by hand on this tag** and written here, per
crew#731 Part 3 — operator-owned, on the round's fleet installed from
`0.1.3-rc3`, before Part 5. The readings are a per-box outcome for every
roster member on each of `crew restart --all`, `crew down` and
`crew reset --cut --all`; a fleet-level summary line alone does not satisfy
the row. A busy box is skipped by `restart --all` and waited for by `down`,
against a real live session. A `reset --cut` refusal is recorded with its
composition, never a bare percentage. A box reset to `armed` comes back on
its first tick with its boot gate passing, read from its own
`boot-check.log` and `duty.log`; a restore falling back to `bootstrapped` or
`pristine` fails. The `duty-snapshot.*` count after the restart is recorded
either way. This row ticks none of Gate B's B1, B3, B4 or B5.

The agreement verdict, `compared` or `could-not-compare`, is recorded either
way. The same operator sitting then records crew#731's Gate B sequence —
including B7, the kimi resume reading that #746 lands ahead of this cut to
make reachable — and starts Gate C's week-scale reading. A negative reading
is recorded rather than hidden; the issue decides whether it blocks promotion
or mints the next candidate defect.
